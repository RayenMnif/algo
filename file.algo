procedure liretab(tab : matrice, n : entier)
debut 
   
   # lire n
   
   repeter
        n <- lire()
        n <- valeur(n)
    jusqu'a n >= 10

   # lire tab 
   
   pour i de 0 a n - 1 faire
    lire(tab[i,i])
   fin_pour 

fin 

n <- 0
liretab(T, n)
